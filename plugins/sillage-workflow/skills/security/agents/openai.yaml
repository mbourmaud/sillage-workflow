interface:
  display_name: "Sillage / Security"
  short_description: "Threat-model software changes proportionally"
  default_prompt: "Use $security for trust boundaries, secrets, identity, or untrusted input."
policy:
  allow_implicit_invocation: true
